{% extends 'base_backoffice.html.twig' %}

{% block title %}Nouvel Événement{% endblock %}

{% block body %}
<div class="dashboard-content-one">
    <!-- Breadcubs Area Start Here -->
    <div class="breadcrumbs-area">
        <h3>Événements</h3>
        <ul>
            <li>
                <a href="{{ path('app_admin_page') }}">Accueil</a>
            </li>
            <li>
                <a href="{{ path('app_evenement_index') }}">Événements</a>
            </li>
            <li>Nouveau</li>
        </ul>
    </div>
    <!-- Breadcubs Area End Here -->

    <!-- Add New Event Area Start Here -->
    <div class="card height-auto">
        <div class="card-body">
            <div class="heading-layout1 mb-4">
                <div class="item-title">
                    <h3><i class="fas fa-plus-circle text-success me-2"></i> Créer un Nouvel Événement</h3>
                </div>
            </div>
            
            {{ form_start(form, {'attr': {'class': 'new-added-form', 'novalidate': 'novalidate', 'enctype': 'multipart/form-data'}}) }}
                <div class="row">
                    <!-- Titre -->
                    <div class="col-12 form-group mb-4">
                        <label class="form-label fw-bold">
                            <i class="fas fa-heading text-primary me-1"></i> Titre de l'événement <span class="text-danger">*</span>
                        </label>
                        {{ form_widget(form.titre, {'attr': {'class': 'form-control form-control-lg'}}) }}
                        {% if form.titre.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {% for error in form.titre.vars.errors %}
                                    <i class="fas fa-exclamation-triangle me-1"></i> {{ error.message }}
                                {% endfor %}
                            </div>
                        {% endif %}
                        <small class="form-text text-muted">Donnez un titre clair et descriptif à votre événement</small>
                    </div>
                    
                    <!-- Description -->
                    <div class="col-12 form-group mb-4">
                        <label class="form-label fw-bold">
                            <i class="fas fa-align-left text-primary me-1"></i> Description <span class="text-danger">*</span>
                        </label>
                        {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': 6}}) }}
                        {% if form.description.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {% for error in form.description.vars.errors %}
                                    <i class="fas fa-exclamation-triangle me-1"></i> {{ error.message }}
                                {% endfor %}
                            </div>
                        {% endif %}
                        <small class="form-text text-muted">Décrivez l'événement en détail (minimum 10 caractères)</small>
                    </div>
                    
                    <!-- Date -->
                    <div class="col-xl-6 col-lg-6 col-12 form-group mb-4">
                        <label class="form-label fw-bold">
                            <i class="fas fa-calendar-alt text-primary me-1"></i> Date de l'événement <span class="text-danger">*</span>
                        </label>
                        {{ form_widget(form.dateEvenement, {'attr': {'class': 'form-control form-control-lg'}}) }}
                        {% if form.dateEvenement.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {% for error in form.dateEvenement.vars.errors %}
                                    <i class="fas fa-exclamation-triangle me-1"></i> {{ error.message }}
                                {% endfor %}
                            </div>
                        {% endif %}
                        <small class="form-text text-muted">Sélectionnez la date prévue pour l'événement</small>
                    </div>
                    
                    <!-- Image -->
                    <div class="col-xl-6 col-lg-6 col-12 form-group mb-4">
                        <label class="form-label fw-bold">
                            <i class="fas fa-image text-primary me-1"></i> Image de l'événement <span class="text-danger">*</span>
                        </label>
                        {{ form_widget(form.imageUpload, {'attr': {'class': 'form-control', 'id': 'imageUpload', 'onchange': 'previewImage(this)'}}) }}
                        {% if form.imageUpload.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {% for error in form.imageUpload.vars.errors %}
                                    <i class="fas fa-exclamation-triangle me-1"></i> {{ error.message }}
                                {% endfor %}
                            </div>
                        {% endif %}
                        <small class="form-text text-muted">
                            <i class="fas fa-info-circle me-1"></i> Formats acceptés: JPEG, PNG, GIF, WebP (max 5 Mo)
                        </small>
                        <!-- Preview -->
                        <div id="imagePreview" class="mt-3" style="display: none;">
                            <img id="previewImg" src="" alt="Aperçu" class="img-thumbnail" style="max-height: 200px;">
                        </div>
                    </div>
                    
                    <!-- Buttons -->
                    <div class="col-12 form-group mt-4">
                        <hr class="mb-4">
                        <button type="submit" class="btn-fill-lg btn-gradient-yellow btn-hover-bluedark me-2">
                            <i class="fas fa-save me-1"></i> Enregistrer l'événement
                        </button>
                        <a href="{{ path('app_evenement_index') }}" class="btn-fill-lg bg-blue-dark btn-hover-yellow">
                            <i class="fas fa-times me-1"></i> Annuler
                        </a>
                    </div>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
    <!-- Add New Event Area End Here -->
</div>

<script>
function previewImage(input) {
    var preview = document.getElementById('imagePreview');
    var previewImg = document.getElementById('previewImg');
    
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function(e) {
            previewImg.src = e.target.result;
            preview.style.display = 'block';
        }
        reader.readAsDataURL(input.files[0]);
    } else {
        preview.style.display = 'none';
    }
}
</script>
{% endblock %}
